{
  "$schema": "https://json.schemastore.org/lerna.json",
  "version": "independent",
  "command": {
    "publish": {
      "conventionalCommits": true,
      "changelog": false,
      "message": "chore(release): publish",
      "allowBranch": "main",
      "ignoreChanges": ["**/*.spec.ts", "**/*.test.ts", "**/test/**"]
    }
  },
  "packages": ["packages/*"]
}
