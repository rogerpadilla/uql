[install]
# Prefer Bun's lockfile
lockfile = true

[test]
# Tests only: hooks are capped at 5s whatever this says, which the `beforeAll` creating the fixture
# tables exceeded on a cold CI runner. `test:bun` passes the same 10s as `--timeout`, which does cover
# hooks, so both budgets are this number.
timeout = 10000
