{
  "private": true,
  "type": "module",
  "sideEffects": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "bun run check && lerna version",
    "check.package": "publint packages/uql-orm && attw --pack --profile esm-only packages/uql-orm",
    "release.patch": "bun run release patch && bun run release.finish",
    "release.minor": "bun run release minor && bun run release.finish",
    "release.major": "bun run release major && bun run release.finish",
    "release.finish": "git push --follow-tags && bun run release.github",
    "release.github": "bun scripts/github-release.ts",
    "publish.orm": "cd packages/uql-orm && bun publish",
    "publish.codemod": "cd packages/uql-codemod && bun publish",
    "check": "bun run lint && bun run ts && bun run ts.rename && bun run test && bun run build && bun run check.package",
    "ts": "tsc --noEmit",
    "ts.rename": "bun scripts/type-rename.ts",
    "ts.perf": "bun scripts/type-perf.ts",
    "build": "bun run --filter '*' build",
    "lint": "oxlint && oxfmt --check",
    "lint.fix": "oxlint --fix && oxfmt",
    "test": "bun run test:vitest && bun run test:bun",
    "test:vitest": "vitest run --coverage",
    "test:bun": "bun test --timeout 10000 --coverage-dir=coverage/bun ./packages/uql-orm/src/bunSql/ ./packages/uql-orm/src/sqlite/sqliteQuerier.bun.test.ts",
    "test.watch": "vitest",
    "test.focus": "vitest run -t 'shouldSetJsonBooleanField'"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.5",
    "@types/bun": "^1.4.2",
    "@types/supertest": "^7.2.1",
    "@vitest/coverage-v8": "^5.0.0",
    "esbuild": "^0.28.2",
    "lerna": "^10.0.1",
    "mongodb-memory-server": "11.2.0",
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0",
    "publint": "^0.3.24",
    "rimraf": "6.1.3",
    "supertest": "^7.2.2",
    "typescript": "^7.0.2",
    "uuid": "^14.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
